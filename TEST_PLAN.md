# 验证清单

- 房主必须先填写自定义问题；空问题、超长问题和控制字符会被拒绝。
- 中文、emoji 与看起来像 HTML 的问题会按纯文本安全显示，并逐字同步给对方。
- 房主创建 12 位随机短房间码；分享链接使用 `https://lsq0000.github.io/r/#房间码`。
- 旧格式 `#room=<PeerJS ID>` 仍可解析；非法或长度不符的 fragment 不会被当成房间。
- 受邀页面可连接房主，第三位参与者会收到房间已满提示。
- 双方确认收到同一个问题前不能提交；问题也包含在承诺哈希中。
- 单方锁定时，对方只看到“已锁定”，看不到数字。
- 双方锁定后，承诺哈希核验通过，再显示两个原始数字。
- `0`、负数、小数和科学记数法可正常揭晓。
- 空值、文字、`Infinity`、`NaN` 和超出有限数范围的输入会被拒绝。
- 任意一方点击“再来一轮”后，两边同时进入新回合，旧答案被清空。
- 双方同时点击“再来一轮”只前进一轮；旧请求不能清空新回合。
- 揭晓倒计时或哈希计算期间断线/重置，不会把上一轮结果写入新回合。
- 对方离开、无效房间、信令失败和脚本加载失败均有可理解的错误提示。
- PeerJS 运行时 ICE 配置同时包含 `stun:` 和 `turn:`；严格 NAT 下可选择 `relay` candidate。
- 单次连接在 12 秒后不会被提前关闭，30 秒超时后访客会分级自动重试，且旧定时器不会关闭新的连接。
- 房主或访客从后台回到前台、恢复网络或从 BFCache 返回时，会恢复信令；房主已经发出的短链接不因恢复而更换。
- 第三位参与者仍会收到房间已满提示，不会因自动重试挤掉已连接的参与者。
- 手机窄屏与桌面宽屏均无横向溢出，键盘焦点和状态播报可用。
